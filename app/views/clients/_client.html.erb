<div id="<%= dom_id client %>">
  <tr>
    <th scope="row"><%= client.id %></th>
    <td><%= client.name %></td>
    <td><%= client.age %></td>
    <% if client.movie_id? %>
          <% @peli = Movie.find(client.movie_id) %>  
          <td> <%= @peli.name %>   </td>
    <% end %>
  </tr>
</div>
